# QuickText フォルダ構成ガイド

このフォルダは3つの用途に分かれています。

---

## 販売用_インストーラー/
**販売サイトにアップロードするファイル**

```
販売用_インストーラー/
├── Mac/
│   ├── QuickText-1.0.0-arm64.dmg        ← Macインストーラー
│   └── QuickText-1.0.0-arm64-mac.zip    ← Mac ZIP版
└── Windows/
    ├── QuickText Setup 1.0.0.exe         ← Windowsインストーラー
    └── QuickText-Windows-Setup-1.0.0.zip ← Windows ZIP版
```

> ビルドし直すと、ここに新しいバージョンが出力されます。

---

## 公開用_ネットアップロード/
**ネット（GitHub、Webサイト等）にアップロードするファイル**

```
公開用_ネットアップロード/
├── README.md                  ← アプリの説明文（使い方・インストール方法）
├── icon-512.png               ← アプリアイコン画像
└── THIRD_PARTY_LICENSES.txt   ← 使用ライブラリのライセンス情報
```

---

## 開発ファイル（ルート直下）
**開発に使うファイル（通常は触らなくてOK）**

```
quicktext/
├── src/                ← ソースコード（アプリの中身）
│   ├── main.ts         ← メインプロセス
│   ├── preload.ts      ← プリロードスクリプト
│   ├── renderer/       ← 画面の表示部分
│   ├── services/       ← AI連携やクリップボード監視
│   └── types/          ← 型定義
├── assets/             ← アイコン画像
├── docs/               ← 仕様書
├── test/               ← テストファイル
├── e2e/                ← E2Eテスト
├── scripts/            ← ビルド用スクリプト
├── public/             ← HTMLテンプレート
├── package.json        ← プロジェクト設定
├── webpack.config.js   ← ビルド設定
├── tsconfig.json       ← TypeScript設定
└── README.md           ← プロジェクト説明
```

---

## よく使う操作

| やりたいこと | コマンド |
|---|---|
| アプリを起動して確認 | `npm start` |
| Mac用インストーラーを作る | `npm run dist:mac` |
| Windows用インストーラーを作る | `npm run dist:win` |
| 両方まとめて作る | `npm run dist:all` |

> コマンドはターミナルで `quicktext` フォルダに移動してから実行してください。
